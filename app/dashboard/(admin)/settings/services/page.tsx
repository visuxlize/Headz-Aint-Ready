import { ServicesSettingsClient } from '@/components/dashboard/ServicesSettingsClient'

export const metadata = {
  title: 'Services & pricing | Headz Staff',
}

export default function ServicesSettingsPage() {
  return <ServicesSettingsClient />
}
