# Supabase (Auth + Database)
# Get these from Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# PostgreSQL connection string (Supabase → Project Settings → Database)
# Use "Connection string" → "URI" → choose "Session" (pooler, port 5432) – free tier and works on Netlify.
# If you only see "Direct" and get ENETUNREACH on Netlify, the app will still load but show "Booking unavailable" until the connection works.
# Pooler hostname must be aws-0-[region] (hyphen after aws: aws-0 not aws0)
DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres

# Optional: for production redirects
# NEXT_PUBLIC_APP_URL=https://your-site.netlify.app
