# Supabase (Auth + Database)
# Get these from Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# PostgreSQL connection string (Supabase → Project Settings → Database)
# Use "Connection string" → "URI" → choose "Session" (pooler, port 5432) – free tier and often works on Vercel.
# If you only see "Direct" and get ENETUNREACH on Vercel, the app will still load but show "Booking unavailable" until the connection works.
DATABASE_URL=postgresql://postgres.[project-ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:5432/postgres

# Optional: for production redirects
# NEXT_PUBLIC_APP_URL=https://your-site.netlify.app
